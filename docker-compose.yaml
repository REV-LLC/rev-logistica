services:
  postgres:
    image: postgres:16
    container_name: rev_postgres
    environment:
      POSTGRES_USER: rev
      POSTGRES_PASSWORD: revpass
      POSTGRES_DB: rev_logistica
    ports:
      - "5432:5432"
    volumes:
      - rev_pgdata:/var/lib/postgresql/data

volumes:
  rev_pgdata:
